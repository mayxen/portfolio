import React from "react";
import {about, experiences, mainBody, skills} from "../../editable-stuff/config";

const project = React.forwardRef(props => {
        return (
            <>

            </>
        );
    }
);

export default project;
